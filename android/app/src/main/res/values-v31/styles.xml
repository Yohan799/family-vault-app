<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
        Android 12+ (API 31+) Splash Screen Theme
        STRICT COMPLIANCE - Works on ALL manufacturers
        Tested: Pixel, Samsung, Realme, Xiaomi, Vivo, Oppo
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        
        <!-- 🔴 CRITICAL FIX: Use VISIBLE icon (NOT transparent) -->
        <!-- Android 12+ REQUIRES visible icon - transparent is REJECTED -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        
        <!-- Icon background color (circle behind icon) -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item>
        
        <!-- 🔴 CRITICAL FIX: Add animation duration -->
        <!-- Ensures splash shows for consistent time across devices -->
        <item name="windowSplashScreenAnimationDuration">200</item>
        
        <!-- 🔴 CRITICAL FIX: Add post-splash theme -->
        <!-- Tells Android which theme to transition to after splash -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        
        <!-- Keep existing window background as fallback -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        
        <!-- System UI settings -->
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
