<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
        Android 12+ (API 31+) Splash Screen Theme
        Using windowBackground with layer-list to center the full image
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        
        <!-- Use the layer-list drawable that centers the image -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        
        <!-- Use transparent icon to disable circular mask, logo shows via windowBackground -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
        
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        
        <!-- Remove the icon background circle - let the image show as-is -->
        <!-- <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item> -->
        
        <!-- Transition to app theme -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        
        <!-- System UI settings -->
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
